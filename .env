TOR_INSTANCES=5
BASE_SOCKS_PORT=9050
BASE_CONTROL_PORT=10050
PROXY_PORT=1080
RELAY_PORT=9999
# Set RELAY_HOST to enable relay mode (e.g. relay_server or an external IP)
# Leave empty to have proxy_pool connect directly through Tor to the destination
RELAY_HOST=